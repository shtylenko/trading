# bb_squeeze_long changelog

| version | notes |
|---|---|
| 0.1.0 | Squeeze pctile + expansion long (broken: scan started after entry window → n=0). |
| 0.1.1 | Fix detection start + causal strict-`<` width pctile; lookback 36; pctile max 0.25. |
